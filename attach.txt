loadstring(game:HttpGet("https://raw.githubusercontent.com/ohmay5/Main/refs/heads/main/attachgun.txt"))()
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local LocalPlayer = Players.LocalPlayer

local Net = ReplicatedStorage:WaitForChild("Modules"):WaitForChild("Net")
local RegisterAttack = Net:WaitForChild("RE/RegisterAttack")
local RegisterHit = Net:WaitForChild("RE/RegisterHit")

local Settings = {
    Enabled = true,
    MinCooldown = 0.28,
    MaxCooldown = 0.60,
    Radius = 55,
    MaxTargets = 3
}

local LastAttack = 0
local CurrentCooldown = Settings.MinCooldown

local function GetTargets()
    local targets = {}
    local char = LocalPlayer.Character
    if not char or not char:FindFirstChild("HumanoidRootPart") then return targets end
    local myPos = char.HumanoidRootPart.Position

    for _, folderName in pairs({"Enemies", "Characters"}) do
        local folder = workspace:FindFirstChild(folderName)
        if folder then
            for _, v in pairs(folder:GetChildren()) do
                if v ~= char and v:FindFirstChild("HumanoidRootPart") and v:FindFirstChild("Humanoid") then
                    if v.Humanoid.Health > 0 then
                        local dist = (v.HumanoidRootPart.Position - myPos).Magnitude
                        if dist <= Settings.Radius then
                            table.insert(targets, {obj = v, dist = dist})
                        end
                    end
                end
            end
        end
    end
    table.sort(targets, function(a, b) return a.dist < b.dist end)
    return targets
end

RunService.Heartbeat:Connect(function()
    if not Settings.Enabled then return end
    
    if tick() - LastAttack >= CurrentCooldown then
        local char = LocalPlayer.Character
        if not char or not char:FindFirstChild("HumanoidRootPart") then return end
        
        local tool = char:FindFirstChildOfClass("Tool")
        local targets = GetTargets()
        
        if #targets > 0 then
            pcall(function()
                -- Tìm Remote M1 của trái (Ưu tiên LeftClickRemote)
                local fruitRemote = tool and tool:FindFirstChild("LeftClickRemote")
                
                if fruitRemote then
                    -- FIX: Bắt buộc phải có tham số (dir, 1) cho Blox Fruits
                    local dir = (targets[1].obj.HumanoidRootPart.Position - char.HumanoidRootPart.Position).Unit
                    fruitRemote:FireServer(dir, 1) 
                else
                    -- Dùng cho Kiếm/Fighting Style
                    local hits = {}
                    for i = 1, math.min(#targets, Settings.MaxTargets) do
                        table.insert(hits, {targets[i].obj, targets[i].obj.HumanoidRootPart})
                    end
                    RegisterAttack:FireServer(0.1) 
                    RegisterHit:FireServer(char.Head, hits)
                end
            end)
            
            CurrentCooldown = math.random() * (Settings.MaxCooldown - Settings.MinCooldown) + Settings.MinCooldown
            LastAttack = tick()
        end
    end
end)
